#include <string.h>

/**
  * _strlen - calculates and returns the length of a string
  * @s: pointer to the string
  *
  * Return: int
  */
int _strlen(char *s)
{
	int len;

	len = strlen(*s);
	return (len);
}
